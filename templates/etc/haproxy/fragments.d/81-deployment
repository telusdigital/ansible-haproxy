frontend deployment-in
  bind :81
  default_backend deployment

backend deployment
  server application-deployment application{%if environment_tier in ['staging', 'production'] %}-direct{% endif %}.{{ project }}-{{ environment_tier }}.{{ domain }}:81
